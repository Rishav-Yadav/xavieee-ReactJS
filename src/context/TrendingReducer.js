const trendingReducer = (state, action) => {
  switch (action.type) {
    case "MOVIES":
      return;
    case "SERIES":
      return;
    default:
      return state;
  }
};
export default trendingReducer;
